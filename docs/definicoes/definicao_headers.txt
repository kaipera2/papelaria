Header: include/produto.h
Propósito: Definir a estrutura de dados central do sistema e expor as funções de regra de negócios.
Structs compartilhadas:
  - Produto.
Defines compartilhados:
  - LIMITE_MAXIMO
  - TAM_NOME
Protótipos Públicos:
  - int adicionar_produto(Produto *estoque, int *total_produtos, Produto novo_produto);
  - int buscar_produto_por_codigo(Produto *estoque, int total_produtos, int codigo_busca);
  - int verificar_status_baixo(Produto p);

---------------------------------------------------------

Header: include/interface.h
Propósito: mostra as funções de interação com o usuário.
Structs compartilhadas:
  - usa a struct Produto
Protótipos Públicos:
  - int exibir_menu();
  - Produto ler_dados_produto();
  - void exibir_sucesso_cadastro(int numero_produto);
  - void exibir_erro_limite();
  - void exibir_erro_produto_nao_encontrado(int codigo);
  - void exibir_detalhes_produto(Produto p);
  - void listar_todos_produtos(Produto *estoque, int total_produtos);